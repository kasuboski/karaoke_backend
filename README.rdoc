== README

Exposes REST routes for a song database

Use regular rails commands to start the project

There is an additional rake task load_songs that will load in a csv file and create or update a song